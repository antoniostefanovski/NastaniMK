@model NastaniMK_2023.Models.ContactModel
@{
    ViewBag.Title = "Контакт";
}

<main>

    <img src="~/Assets/contact2.png"/>

    <div class="mt-3">
        <h2 id="title" class="textColor">Наш Контакт</h2>
        <p class="lead">Доколку имате некои прашања или забелешки слободно <span class="textColor fw-semibold">контактирајте не</span>.</p>
        <div class="mt-3">
            <p class="lead p-3">
                <span class="textColor"><i class="bi bi-telephone-fill"></i></span> Tel. <span class="textColor fw-semibold">075/738-257</span> <br />
                <span class="textColor"><i class="bi bi-envelope-at-fill"></i></span> Email <span class="textColor fw-semibold">contact@nastani.mk</span>
            </p>
        </div>
    </div>

    <div class="mt-3">
        <p class="lead">Организатор сте и заинтересирани сте за соработка ? <span class="textColor fw-semibold">Контактирајте не по email</span>.</p>
        <div class="mt-3">
            <p class="lead p-3">
                <span class="textColor"><i class="bi bi-envelope-at-fill"></i></span> Email <span class="textColor fw-semibold">businessinquiries@nastani.mk</span>
            </p>
        </div>

        <p class="lead">Или пополнете ја формата и ние <span class="textColor fw-semibold">ќе ве контактираме</span>.</p>

        @using (Html.BeginForm("FormContact", "Home"))
        {
            <div class="form-group">

                <div>
                    @Html.Label("Вашето Име", new { @class = "form-label" })
                    @Html.TextBoxFor(u => u.UserName, null, new { @class = "form-control" })
                </div>

                <div>
                    @Html.Label("Име на организацијата", new { @class = "form-label" })
                    @Html.TextBoxFor(u => u.OrganizationName, new { @class = "form-control" })
                </div>

                <div>
                    @Html.Label("Контакт (email)", new { @class = "form-label" })
                    @Html.TextBoxFor(u => u.Email, new { @class = "form-control" })
                </div>

                <div>
                    @Html.Label("Порака", new { @class = "form-label" })
                    @Html.TextAreaFor(u => u.Message, new { @class = "form-control" })
                </div>

                <div class="mt-2">
                    <button class="btn btn-danger">Контакт</button>
                </div>

            </div>
        }

    <div class="mt-5">
        @Html.ActionLink("Врати се назад", "Index", "Home", new { @class = "btn btn-primary" })
        <a href="/Events/IndexUser" class="btn btn-primary">Настани</a>
    </div>

    </div>
</main>